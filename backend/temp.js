   // console.log(f_Username);
    // const hashedPassword = await bcrypt.hash(f_Pwd, 10);
    // const loginData = { ...req.body, f_Pwd: hashedPassword };
    // const newlogin = new LoginModel(loginData);
    // await newlogin.save();
    // return res.status(200).json({message : "record added now ."});
